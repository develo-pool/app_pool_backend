spring:
  data:
    web:
      pageable:
        one-indexed-parameters: true
        default-page-size: 10

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

  profiles:
    include: jwt, develop, prod
    active: ${profile}

file:
  dir: /Users/johnn/Desktop/img/